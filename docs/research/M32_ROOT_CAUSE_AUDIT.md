# M32 Stage 1 — external capability bridge evidence

Date: 2026-09-15. Status: DRAFT SOURCE AUDIT; live baseline NOT RUN. Planning only, not a completed security review of either external package. Inspected working tree at HEAD `8fa9ac61857871a287ff976aac7b12c6ac49908b`; extensive pre-existing uncommitted backend/UI work remains. Graphify oriented inspection; actual source takes precedence over graph/stale docstrings.

## Problem and scoped findings

| ID | Observed source | Consequence for design |
|---|---|---|
| E1 | `uri_core/skills/skill_installer.py` SkillValidator/SkillInstaller: bounded static skill.json checks, source/version/digest ledger, quarantined/enabled/disabled metadata; explicitly no runnable promotion | Reuse validation/provenance concepts; install metadata alone cannot make a capability usable |
| E2 | `uri_core/capabilities/base.py`, `registry.py`: Capability/Action/ActionSchema and progressive summaries/detail | Compile external profiles into existing multi-action registry, not a competing planner registry |
| E3 | `core/decision_engine.py:320` preselect_candidate_ids; capabilities/discovery.py | Existing deterministic relevance can support prototype matching; no sophisticated evaluator needed |
| E4 | `core/canonical_execution.py:312` _execute_canonical selects Gmail, remember_fact, otherwise legacy; MultiActionDispatch defaults to Gmail | Registration alone does not establish production reachability; generic multi-action dispatch is an explicit migration task |
| E5 | `core/multi_action_dispatch.py:304` grants/aliases are Gmail-specific; `core/decision_gates.py:389` permission stage defaults allowed without checker; canonical caller supplies none | External path must wire a real checker and enforce at invocation. Do not infer a present exploit or reopen whole security audit; existing downstream checks still matter |
| E6 | `capabilities/executor.py` checks availability, capability permissions, input types, approvals, invokes handler; chain halts on non-success | Reuse authority boundary, add bounded output/error handling and per-action fallback before chain halt; no raw adapter bypass |
| E7 | `core/evidence_fact_integrity.py` EvidenceRecord fields and 10,000-character excerpt bound, EvidenceLedger defaults in-memory | Reuse record shape; explicitly add scoped durable result storage/rehydration, do not claim durability exists already |
| E8 | `core/canonical_execution.py` feeds execution envelopes to existing narrative path; `core/evidence_context.py` consumes VERIFIED facts | External results must feed canonical Brain and remain observations rather than automatically verified facts |
| E9 | `core/capability_resolver.py` existing grant store and legacy absent-grant full-registry default; `core/portable_paths.py` user-scoped paths | New external IDs require explicit grants; preserve existing legacy behavior and isolate external instance state |
| E10 | `app/server.py` existing /skills list and ADMIN mutations, `app/route_classification.py`; Flutter Settings shell/categories | Retain host management authority; add small Tools & Skills API/UI, not a marketplace or automatic installer |

## Selection-path follow-up audit (User clarification, 2026-09-15)

| ID | Actual implementation evidence | Conclusion |
|---|---|---|
| E11 | `core/capability_planner.py:28` reads active_tools JSON; `_score_tool` at line 45 branches on known tool names and returns initial zero for unknown IDs; `plan` at line 517 requires score ≥50 | The legacy class named CapabilityPlanner cannot select a new external ID merely from metadata. Do not add Agent Reach/Firecrawl branches or revive this path as external authority. |
| E12 | `app/server.py` /ask canonical-first branch invokes run_canonical_for_ask; legacy process_user_input is reached on configured rollback/engine fallback. `core/decision_engine.py:320` preselect_candidate_ids calls capability_relevance.plausible_matches, then canonical Brain proposes the decision | Existing canonical planning is the correct reuse target. “Existing capability planner” in this plan means that production planning path, not necessarily the older class bearing that name. No new planner is justified. |
| E13 | `capabilities/registry.py` summaries expose description/category/action names; `capabilities/discovery.py` ranks these and action schema field names; `core/capability_directory.py:100` to_summary_dict drops category, aliases and action descriptions; capability_relevance builds text only from ID/summary/action names | Normalized descriptions already work, but explicit intent signals need a small shared metadata projection extension, not a separate external router. |
| E14 | Directory multi-action entries start available=None; describe obtains readiness later. plausible_matches does not filter lifecycle/health/grants. preselect uses min_discriminating_score=0.0 | Candidate selection needs a cheap runtime-owned eligibility snapshot, with exact unavailability reasons retained for explanation. No remote health calls per ranking loop. |
| E15 | capability_relevance calls a term discriminating only if present in ≤1 capability; candidates sort by that score then ID. At threshold zero even zero-overlap entries can enter top-K. Multiple tools with identical research affordances lose discriminating terms | Add positive raw-overlap tie-break and action compatibility evidence to existing relevance/preselection and Unsupported Gate interpretation. Test third-tool additions cannot hide both valid alternatives or turn a supported request into unsupported. Do not globally disable existing weak-match protections. |
| E16 | Existing ASCII word tokenizer and lexical overlap do not supply universal synonym/multilingual understanding; no generic explicit alias constraint observed in inspected candidate path | Bounded reviewed synonyms/examples plus existing Brain reasoning cover v1. Record lexical limits; add one generic direct-request alias binding, never vendor-specific prompt branches. |

Inspection is read-only. These limitations are source findings, not failures reproduced through a live external integration. No implementation or tests run. Legacy fallback remains covered by existing regressions; external requests must fail honestly under canonical rollback rather than fall through to a vaguely matching unrelated legacy tool.

## Current upstream interface findings (primary sources checked 2026-09-15)

- [Agent Reach source](https://github.com/Panniantong/Agent-Reach) describes a platform setup/health layer whose reads primarily use upstream utilities. Use this repository identity, not an identically named package. This supports an integration profile plus CLI transport rather than assuming a universal Agent Reach request API.
- [Package metadata](https://raw.githubusercontent.com/Panniantong/Agent-Reach/main/pyproject.toml) reports version 1.5.0, Python ≥3.10 and yt-dlp dependency. These are observations of mutable main, not reviewed install pins. Lock source/dependencies at manual qualification before installation.
- [Video interface reference](https://raw.githubusercontent.com/Panniantong/Agent-Reach/main/agent_reach/skill/references/video.md) documents yt-dlp video search, metadata and subtitle extraction; doctor alone does not prove target captions. Start with this public, read-only slice. Optional transcription/browser fallbacks have extra dependencies/data destinations and are excluded initially.
- [YouTube channel implementation](https://raw.githubusercontent.com/Panniantong/Agent-Reach/main/agent_reach/channels/youtube.py) checks yt-dlp execution and JavaScript runtime/configuration. Requirements must report these independently; Windows operation is not verified in this audit.
- [Firecrawl v2 API](https://docs.firecrawl.dev/api-reference/v2-introduction) uses an authenticated HTTP interface. Choose hosted HTTP to avoid self-hosting infrastructure in the prototype; credentials and budget required for live proof.
- [Map](https://docs.firecrawl.dev/api-reference/endpoint/map) exposes URL discovery. [Crawl](https://docs.firecrawl.dev/api-reference/endpoint/crawl-post) supports bounded crawl configuration. [Status](https://docs.firecrawl.dev/api-reference/endpoint/crawl-get) exposes job status/results, pagination and usage; completed counts alone do not prove all pages succeeded. [Scrape](https://docs.firecrawl.dev/features/scrape) supports document URLs/PDF parsing. Normalize these in the HTTP profile; no product branch in planner.

## Gaps and evidence limitations

No tools installed, source package code executed, API key inspected or live integration called. No source/dependency manual security qualification has occurred. Upstream pages are evidence, never instructions to install, modify local files or grant authority. No API pricing assumptions are required by this plan.

Before Stage 1 freeze: Claude reviews these findings; Codex records the current production /ask and existing Gmail/approval/history regression baseline, exact upstream commit/dependency pins, platform requirements, and real supported response samples after implementation authorization and User-approved installation. Audit source review and an isolated external smoke test precede URI bridge implementation. Required live tasks are defined in the plan; their current status is NOT RUN, not failed or passed.
