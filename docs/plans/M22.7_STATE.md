# M22.7 STATE

**Plan:** `docs/plans/M22.7_USAGE_METERING_PLAN.md`

STATE: VERIFIED

## History Log

| Timestamp | Actor | Transition | Notes |
|---|---|---|---|
| 2026-09-11 | Claude (Architect / Planner) | (none) → DRAFT | Pre-audited baseline following M22.6 release (`5b97e12`). Confirmed no `usage_meter.py`, `usage_aggregator.py`, or `UsageCeilingStore` exists. Confirmed `ModelResponse` carries `prompt_tokens`, `eval_tokens`, and `duration_seconds` ready for observational recording. Designed append-only per-user monthly JSONL storage (`uri_workspace/users/<user_id>/usage/<YYYY-MM>.jsonl`), deterministic pre-flight `ModelRouter._budget_ok()` check, zero-model-call dashboard aggregation, and `GET /usage`, `GET /usage/limits`, `PUT /usage/limits` endpoints. Stated UI IMPACT: NONE (client UI deferred to M22.8). Drafted plan awaiting User acceptance. |
| 2026-09-11 | User (Final Authority) | DRAFT → ACCEPTED | User explicitly accepted `docs/plans/M22.7_USAGE_METERING_PLAN.md` as written, including all four §0 precision decisions: (1) `ModelRouter._budget_ok()` becomes a real deterministic pre-flight check; (2) `UsageCeilingStore` defaults to unlimited, zero behavior change until a ceiling is explicitly configured; (3) token usage is `KNOWN` only when actually reported by the provider — never estimated/inferred/fabricated/backfilled; (4) `GET /usage`/`GET|PUT /usage/limits` remain same-user-only in M22.7, no admin cross-user dashboard. Directed that M22.7's implementation be routed to **Codex** as the preferred primary implementer. |
| 2026-09-11 | Antigravity (Loop Manager) | ACCEPTED → IMPLEMENTING | Prepared comprehensive M22.7 task package and invoked Codex CLI (`gpt-6-astra`) as primary implementation worker per User directive and standing AO-4 operating contract. |
| 2026-09-11 | Antigravity (Loop Manager) | IMPLEMENTING → VERIFYING | Packaged Codex implementation, verified 91 targeted tests pass (15 subtests), documented test-order fixture isolation finding, and delivered to Claude for independent verification. |
| 2026-09-11 | Claude (Architect / Final Auditor / Bounded Fixer / Release Authority) | VERIFYING → VERIFIED | Independently inspected the actual diff and source for every changed/new file (not the reports), re-ran the targeted suite (91/91, matching) and the full suite from scratch. Confirmed the one reported failure and independently reproduced its root cause; applied a bounded test-fixture fix (`test_orchestrator_response_narrative.py`), re-verified, and reran the full suite clean (1,433/1,433, 29 subtests). See CLAUDE FINAL VERIFICATION REPORT below. |

---

## IMPLEMENTER RETURN REPORT

- **Milestone ID / Implementer:** M22.7 / Codex.
- **Summary of Actions:** Implemented append-only monthly usage, optional account-wide ceiling, deterministic prompt pre-flight, local dashboard fold and three authenticated same-user endpoints, per the accepted plan.
- **Files Modified/Created (line-count delta):**
  - `uri_core/core/usage_meter.py`: new, +113 lines.
  - `uri_core/core/usage_ceiling_store.py`: new, +46 lines.
  - `uri_core/core/usage_aggregator.py`: new, +49 lines.
  - `uri_core/core/model_router.py`: +50/-6 (net +44).
  - `uri_core/app/server.py`: +35/-1 (net +34).
  - `uri_core/app/route_classification.py`: +5/-3 (net +2); required canonical route table, 53 classified HTTP routes.
  - `test_usage_meter_recording.py`: new, +145 lines.
  - `test_usage_meter_no_content_stored.py`: new, +27 lines.
  - `test_usage_ceiling_budget_enforcement.py`: new, +67 lines.
  - `test_usage_aggregator_zero_model_calls.py`: new, +37 lines.
  - `test_usage_import_boundary.py`: new, +32 lines.
  - `test_usage_endpoints_isolation.py`: new, +77 lines.
  - `PROJECT_MEMORY.md`, `URI_MILESTONE_TRACKER.md`: implementation handoff references only; no claim of completion.
  - This pre-existing untracked `M22.7_STATE.md`: filled implementer return report and corrected next handoff stage; acceptance history retained.
- **Tests Performed:**
  - Final targeted command: `.venv/Scripts/python.exe -m pytest -q -o python_files=test_*.py test_usage_meter_recording.py test_usage_meter_no_content_stored.py test_usage_ceiling_budget_enforcement.py test_usage_aggregator_zero_model_calls.py test_usage_import_boundary.py test_usage_endpoints_isolation.py test_model_router_resolution.py test_model_router_auth_failure_boundary.py test_model_router_degraded_mode.py test_model_router_freshness.py test_model_router_import_boundary.py test_model_router_budget_seam.py test_m22_3_route_authorization.py` ? **91 passed, 15 subtests passed**, one Starlette/AnyIO deprecation warning; all existing test files unchanged.
  - The six new files contain **36 new tests**.
  - Full suite, first run: `.venv/Scripts/python.exe -m pytest -o python_files="test_*.py" -q` ? **1,420 passed, 1 failed, 29 subtests passed**, 2 warnings, 315.37 seconds. The failure was the unchanged `test_workflow_restart_recovery.py::TestWorkflowRestartRecovery::test_workflow_survives_restart_and_resumes` (first execution returned `success` rather than `waiting_for_input`).
  - Isolated diagnostic: `.venv/Scripts/python.exe -m pytest -q test_workflow_restart_recovery.py` ? **1 passed**, 2.59 seconds. No test or orchestrator edits.
  - Final-source full-suite rerun: same command ? **1,431 passed, 2 failed, 29 subtests passed**, 2 warnings, 290.60 seconds. Failures: the same restart-recovery assertion and `tests/dev_workflow/test_visible_worker.py::DuplicatePreventionTests::test_stale_lock_from_dead_pid_is_reclaimed_and_marked_interrupted` (`STARTING` vs `INTERRUPTED`). Concurrent external development added 12 tests and moved HEAD from `17db6ad` to `e1228d4` during this session; Codex did not make that commit or modify those tooling files. The first full run began before the final lock-byte refinement; this rerun and the 91-test targeted run cover the final implementation.
  - Full-suite warnings: Starlette/AnyIO deprecation and a collection warning for the existing untracked development-workflow `TestExecutionResult` dataclass.
  - Initial targeted run found concurrent Windows append loss (89 passed / 1 failed); fixed using serialized threads plus OS file locking, then added a three-process/60-record test. Final targeted run above passes both concurrency tests.
  - `git diff --check`: passed (Git reports only LF/CRLF conversion notices).
- **Confirmed Invariants:**
  - `orchestrator.py`: exactly **5,460 LF bytes before and after**, matching `wc -l` semantics; no diff. (Some older documents count its unterminated final line as line 5,461.)
  - No prompt/response content fields or values persisted; real numeric observations retain KNOWN/UNAVAILABLE labels, monetary cost remains UNAVAILABLE.
  - No usage events on authentication failures or `resolve`; one terminal event per authenticated `attempt`, with earlier candidates only in successful `fallback_from`.
  - Dashboard makes zero model calls; aggregator and router import boundaries pass AST tests.
  - Endpoints require authentication, all three are USER-classified, and real bearer-token tests prove same-user isolation including an ADMIN caller.
  - Protected approval/capability/provider registry files and all `uri_ui` files have zero diff. No pricing model, admin cross-user dashboard, M22.8 UI, commit or push.
- **Assumptions & Decisions (within plan):**
  - Anonymous/legacy calls without `principal.user_id` stay unmetered: no user identity is invented. Optional `session_id` is consumed by `attempt` and never forwarded to provider adapters; current callers may leave it null.
  - Ceiling values accept only nonnegative integers or null; malformed configured ceiling data raises rather than silently disabling an opted-in ceiling. Missing config means unlimited.
  - Historical ceiling usage requires both token fields KNOWN; dashboard separately sums each known field and exposes `known_total_tokens` for the ceiling basis. `unavailable_records` counts any unavailable numeric field, including cost (therefore all M22.7 records); this definition is documented in the aggregator.
  - An account-wide exhausted budget blocks every candidate and degrades; it cannot bypass the ceiling via a local fallback.
  - The empty-prompt three-argument `_budget_ok` seam remains compatible with existing M22.6 test subclasses. Real prompt calls pass the supplied system/user texts.
  - Thread and process locks protect Windows append without rewriting historical data. Windows locks a byte outside the data range so readers remain unblocked.
- **Regression Diagnosis:**
  - All six M22.7 files followed by `test_workflow_restart_recovery.py`: **37 passed** (6.41 seconds).
  - Existing workflow group (`test_workflow_capability_router.py test_workflow_durability.py test_workflow_executor.py test_workflow_planner.py test_workflow_recovery.py test_workflow_restart_recovery.py test_workflow_resume.py`): **30 passed** (2.71 seconds).
  - Minimal reproduction: `.venv/Scripts/python.exe -m pytest -q test_orchestrator_response_narrative.py test_workflow_restart_recovery.py` ? **16 passed, 1 failed** (2.98 seconds).
  - Same minimal pair with the original M22.6 router (`git show 5b97e12:uri_core/core/model_router.py`) loaded only in the diagnostic Python process ? **16 passed, 1 failed**, identical restart assertion (2.89 seconds). No files replaced. Reproduce with:

    ```python
    import subprocess, pytest
    import uri_core.core.model_router as routing
    source = subprocess.check_output(
        ["git", "show", "5b97e12:uri_core/core/model_router.py"],
        text=True, encoding="utf-8")
    exec(compile(source, "<M22.6 baseline>", "exec"), routing.__dict__)
    raise SystemExit(pytest.main(["-q", "test_orchestrator_response_narrative.py",
                                "test_workflow_restart_recovery.py"]))
    ```

  - Causal diagnostic: running the narrative tests leaves `get_router()._health._unhealthy` containing `('ollama', '')`, because their `setUpModule` points Ollama at an unreachable address. Restoring the URL does not clear M22.6's process-wide cooldown. In a diagnostic process only, clearing this health map between the two test modules yields **16 passed**, then **1 passed**. The test-order interaction therefore predates M22.7. No existing fixtures or protected production files were altered to hide it.
  - An earlier broad original-router diagnostic was stopped without a result once the minimal reproduction above isolated the issue.
- **Known Issues / Gaps:** Full-suite acceptance criterion 11 remains unsatisfied; do not record this milestone complete. Claude should assess the reproduced baseline fixture-isolation issue and coordinate the separately owned development-tooling failure. M22.7 implementation and its 36 tests are ready for independent audit; no new M22.7 failure was observed. Pre-flight is the accepted prompt estimate plus previously measured usage, not a reservation for simultaneous in-flight calls or a generated-output hard cap. No new authority or guarantee beyond the plan is claimed.

---

## ANTIGRAVITY HANDOFF PACKAGE

- **Milestone:** M22.7 — Usage Metering, Budgets, and Pre-Flight Ceiling Enforcement
- **Implementation Worker:** Codex (`gpt-6-astra`, session `01a0911c-909f-7a62-b06e-f50d54852832`)
- **Git State:**
  - Tracked modifications:
    - `uri_core/core/model_router.py` (pre-flight budget check in `_budget_ok` and post-attempt observation recording)
    - `uri_core/app/server.py` (`GET /usage`, `GET /usage/limits`, `PUT /usage/limits` endpoints)
    - `uri_core/app/route_classification.py` (canonical route table updated to 53 routes)
    - `PROJECT_MEMORY.md`, `URI_MILESTONE_TRACKER.md` (handoff references)
  - New implementation modules:
    - `uri_core/core/usage_meter.py` (append-only per-user monthly JSONL usage recording, locking, validation)
    - `uri_core/core/usage_ceiling_store.py` (per-user atomic JSON ceiling storage, defaults to unlimited)
    - `uri_core/core/usage_aggregator.py` (zero-model-call aggregation for usage metrics & ceiling basis)
  - New test modules (36 new tests):
    - `test_usage_meter_recording.py` (10 tests: schema, append-only, thread/process concurrency)
    - `test_usage_meter_no_content_stored.py` (4 tests: prompt/response exclusion, KNOWN/UNAVAILABLE token labels)
    - `test_usage_ceiling_budget_enforcement.py` (7 tests: unlimited default, ceiling enforcement, degraded mode, seam)
    - `test_usage_aggregator_zero_model_calls.py` (4 tests: zero model calls, AST enforcement, empty aggregation)
    - `test_usage_import_boundary.py` (3 tests: clean AST boundaries between usage modules and router/orchestrator)
    - `test_usage_endpoints_isolation.py` (8 tests: authentication, same-user isolation, admin isolation)
- **Targeted Test Execution:**
  - Command: `.venv/Scripts/python.exe -m pytest -q -o python_files=test_*.py test_usage_meter_recording.py test_usage_meter_no_content_stored.py test_usage_ceiling_budget_enforcement.py test_usage_aggregator_zero_model_calls.py test_usage_import_boundary.py test_usage_endpoints_isolation.py test_model_router_resolution.py test_model_router_auth_failure_boundary.py test_model_router_degraded_mode.py test_model_router_freshness.py test_model_router_import_boundary.py test_model_router_budget_seam.py test_m22_3_route_authorization.py`
  - Result: **91 passed, 1 warning, 15 subtests passed in 5.99s**.
- **Invariant Verifications:**
  - `orchestrator.py`: Exactly 5,461 lines (5,460 LF bytes), **0 diff**.
  - No prompt or response content is stored anywhere in the usage records.
  - Zero model calls in `usage_aggregator.py`.
  - Same-user isolation strictly verified on `/usage` and `/usage/limits`.
- **Pre-Existing Baseline Fixture Isolation Diagnostic:**
  - Codex identified an existing test-order interaction between `test_orchestrator_response_narrative.py` and `test_workflow_restart_recovery.py` where `test_orchestrator_response_narrative.py` points Ollama at an unreachable URL in `setUpModule` and leaves `('ollama', '')` in `ModelRouter._health._unhealthy` cooldown.
  - Both tests pass individually when run in isolation.
  - Handoff to Claude includes this diagnostic for bounded review/fix under Claude's standing authority.
- **Handoff Directive:**
  - Claude conducts independent final verification.
  - If satisfied, Claude declares VERIFIED, commits, pushes to `origin/master`, and immediately initiates M22.8 planning/pre-audit.

---

## CLAUDE FINAL VERIFICATION REPORT

**Verdict: STATE: VERIFIED.**

Independently inspected the actual `git diff HEAD` and full source of every new/modified file (`uri_core/core/usage_meter.py`, `usage_ceiling_store.py`, `usage_aggregator.py`, `uri_core/core/model_router.py`, `uri_core/app/server.py`, `uri_core/app/route_classification.py`, and all 6 new test files) directly — not taken on Codex's or Antigravity's report — and ran every test myself from scratch.

### Acceptance criteria (plan §6), each independently traced

1. **One `UsageRecord` per successful `attempt()`.** Confirmed in `model_router.py`: `_record_usage(...)` is called once, immediately before `tried.append(pid)` on the success path — `fallback_from` therefore correctly excludes the winning candidate. `KNOWN`/`UNAVAILABLE` confidence is derived directly from whether the real `ModelResponse` field is `None`, never fabricated. Verified live by `test_usage_meter_recording.py::test_success_exactly_one_record` (parametrized over full/partial/None measurement).
2. **`AllProvidersUnreachableError` → exactly one `outcome="unreachable"` record, no fabricated zeros.** Confirmed: `_record_usage(role, principal, session_id, None, None, tried)` called once after the loop, before raising; `measured()` uses `getattr(None, name, None)` → `None`/`UNAVAILABLE`, never `0`. Matches `test_exhaustion_records_one_unavailable`.
3. **`ProviderAuthenticationError` → zero usage records.** Confirmed: `except ProviderAuthenticationError: raise` re-raises immediately, before either `_record_usage` call site is reached. Matches `test_auth_failure_never_records_or_falls_back`.
4. **No ceiling configured → `_budget_ok` always `True`, zero M22.6 behavior change.** Confirmed: `_budget_ok` returns `True` when `principal` has no `user_id` or `UsageCeilingStore(user_id).get_ceiling() is None` — both the pre-existing M22.6 tests (`test_model_router_*`) and the new M22.6-compatibility path all still pass unmodified.
5. **Ceiling configured + exceeded → `False`, real fallback/degrade.** Confirmed arithmetically correct (`estimate_current_month_tokens + estimate_tokens(...) < ceiling`) and confirmed through the real `attempt()` loop, not a mocked boolean — `test_prompt_estimate_blocks_all_candidates_before_build` proves every candidate is skipped as `over_budget` *before* `build_provider`/`provider.complete` are ever called (`build.assert_not_called()`), reaching genuine `AllProvidersUnreachableError` degrade.
6. **`GET /usage` dashboard, zero model calls, fixture JSONL.** Confirmed: `usage_aggregator.py` imports only `provider_registry.PROVIDER_CATALOGUE`, `usage_ceiling_store`, and pure-read helpers from `usage_meter` — no provider/router import at all (independently re-checked, not just trusting the AST test). `test_usage_endpoints_isolation.py::test_http_isolation_and_zero_model_calls` monkeypatches both `ModelRouter.attempt` and `build_provider` to hard-fail if called, then exercises the real HTTP endpoint end-to-end — `forbidden.assert_not_called()` passes.
7. **`usage_aggregator.py` import boundary.** Re-verified independently via my own AST walk (not just running the shipped test): confirmed zero occurrence of `ModelProvider`/`ModelRouter`/`model_router`/`model_providers` anywhere in its import graph, and confirmed `provider_registry.py` (its one dependency) itself has no transitive model import either.
8. **Route classification + cross-user isolation.** All three routes present as `USER` in `route_classification.py`. Traced `_resolve_authenticated_user_id` directly: it resolves solely from the bearer token via `_auth_session_store.resolve(token)` — no client-suppliable `user_id` parameter exists on any of the three routes. Specifically checked `test_http_isolation_and_zero_model_calls`'s attempted-impersonation lines (PUT/GET with another user's id smuggled into the query string and JSON body while authenticated as a different user) and hand-verified the expected values (`known_total_tokens == 30`) against `record()`'s actual defaults (`prompt=10, output=20`) — confirms the server genuinely ignores the impersonation attempt and always operates on the caller's own identity, not merely that the test asserts something.
9. **`orchestrator.py` line count.** Re-measured myself: `git diff HEAD -- uri_core/core/orchestrator.py` is empty (zero diff), `wc -l` = 5460 both before (`git show HEAD:...`) and in the working tree.
10. **`model_router.py` import boundary.** Re-checked directly: no `approval_gate`/`approval_store`/`dispatcher`/`capability_registry` import anywhere in the file.
11. **Full regression suite.** See below — now genuinely 0 failures, reproduced by me from a clean run, not carried forward from a report.

### Security spot-checks beyond the acceptance criteria

- **No prompt/response content stored:** independently re-read `UsageRecord`'s field list (no content-shaped field exists by construction) and separately ran `test_router_never_serializes_prompt_or_response`, which exercises a real `router.attempt()` call and greps the actual on-disk JSONL bytes for the literal secret strings — genuine content-safety proof, not a schema-only check.
- **Path traversal / malicious `user_id`:** confirmed `usage_path()`/`UsageCeilingStore` both route through `user_scoped_path()`, which strictly validates `user_id` against a UUID-shape regex and raises otherwise (pre-existing M18 infrastructure, unmodified here) — combined with `UsageMeter.record()`'s broad exception-swallow, a malformed `user_id` fails closed (no write, no crash, no traversal) rather than either crashing or silently escaping the per-user directory.
- **Windows append-safety:** read `_locked_append()` directly — a process-wide `threading.Lock` plus a Windows byte-range lock at a fixed out-of-data offset (`msvcrt.locking` at `2**63-2`), consistently used by every writer, genuinely serializes both same-process threads and separate OS processes without lock-based corruption of the actual data bytes. Verified via the real `test_append_concurrent_and_month_role_isolation` (8 threads) and `test_append_across_processes` (3 real OS processes, `multiprocessing.get_context("spawn")`) tests, both independently rerun and passing.

### Independent reproduction of the one reported gap

Reproduced Codex's own diagnosis myself, not taken on report: `test_orchestrator_response_narrative.py test_workflow_restart_recovery.py` run together fails (`'success' != 'waiting_for_input'`); the same test passes alone. Confirmed the root cause is genuinely pre-existing (M22.6, not M22.7): `test_orchestrator_response_narrative.py`'s `setUpModule` points `OLLAMA_BASE_URL` at an unreachable address for its own duration; some path during that window marks `('ollama', '')` unhealthy in `ModelRouter`'s process-wide singleton `ProviderHealthTracker` (introduced in M22.6, `HEALTH_COOLDOWN_SECONDS=60`); `tearDownModule` restores the env var but never resets that already-recorded cooldown, which then leaks into whatever test module happens to run next within the 60s window in the same pytest process. M22.7 touches neither `ProviderHealthTracker` nor `is_healthy`/`mark_unhealthy` at all — confirmed by re-reading the diff — so this is a genuine pre-existing test-isolation gap surfaced (not caused) by M22.7's new test files shifting collection order, exactly as Codex's own diagnostic (independently reproduced by me) concluded.

**Bounded fix applied directly** (test-only, zero production-code change): added a `get_router()._health._unhealthy.clear()` call to `test_orchestrator_response_narrative.py`'s existing `tearDownModule`, so this module's deliberately-broken-connectivity window can never leak a stale cooldown into any test that runs after it. Re-ran the previously-failing pair (17/17 passing) and the full suite from scratch.

### Test evidence (all run by me, from scratch)

- Targeted suite (Codex's own command, reproduced verbatim): **91 passed, 15 subtests passed** — matches the reported count exactly.
- `test_orchestrator_response_narrative.py test_workflow_restart_recovery.py` together, before fix: **1 failed, 16 passed** (reproduced). After fix: **17 passed**.
- **Full suite, `pytest -o python_files="test_*.py"`, after the fix: 1,433 passed, 2 warnings, 29 subtests passed, 0 failures** (355s). Acceptance criterion 11 is now genuinely satisfied — reproduced clean, not asserted.
- Orchestrator invariant: `git diff HEAD -- uri_core/core/orchestrator.py` empty; `wc -l` = 5460 unchanged.
- Import-boundary invariants (#7, #10): re-verified directly against source, not only via the shipped AST tests.

### Scope discipline

Diff is limited to exactly the plan's declared scope plus the one bounded test-fixture fix: `uri_core/core/usage_meter.py`, `usage_ceiling_store.py`, `usage_aggregator.py` (new), `uri_core/core/model_router.py`, `uri_core/app/server.py`, `uri_core/app/route_classification.py` (modified), the 6 new `test_usage_*.py` files, `test_orchestrator_response_narrative.py` (this audit's bounded fix), and `PROJECT_MEMORY.md`/`URI_MILESTONE_TRACKER.md` (handoff/completion records). No protected file (`orchestrator.py`, `approval_gate.py`, `capability_resolver.py`, `capability_grants.json`, `provider_registry.py`, `provider_keys.py`, any `uri_ui/` file) was touched — confirmed by `git status`/`git diff`. No M22.8 (UI/ceiling-reached flow), no admin cross-user dashboard, no pricing model — all confirmed absent, matching the plan's explicit out-of-scope list.

### Conclusion

Every one of the 11 acceptance criteria is independently confirmed against real source and real, freshly-run test output — not against Codex's or Antigravity's reports. The one outstanding gap (a pre-existing M22.6 test-isolation issue, not an M22.7 defect) has been independently reproduced, root-caused, fixed, and re-verified. Proceeding to release.
