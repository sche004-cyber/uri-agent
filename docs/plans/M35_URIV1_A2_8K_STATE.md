# M35 URI v1 — A2.8K STATE

Current state: **COMPLETE — A2.8K CLOSED / ACCEPTED** (2026-09-24, following R2's independent `ACCEPT` verdict. Closure committed and pushed to `origin/master`.)

Owner: none — milestone closed. Next: Claude drafts the next milestone plan from freshly inspected post-release repository state.

**Final qualified result:**
- H1 = `PARTIALLY_SUPPORTED`
- H2 = `PARTIALLY_SUPPORTED`
- H3 = `PARTIALLY_SUPPORTED` (R2 repaired the H3 activation-boundary defect; domain-relative ranking now activates only on genuine candidate-id-set narrowing)
- H4 = `PARTIALLY_SUPPORTED` (inherits H3's repair; additive over H1/H2/H3 as measured)
- Decision accounting is complete (raw scan reconciles exactly with aggregate changed-row keys/contents for all six variants across all 606 decision keys).
- Determinism verified (two fresh in-memory battery runs reproduce all 3,636 decision rows + 19 S-E records exactly; zero mismatches).
- No new ICBs introduced by R2 relative to R1 or to H0 (0 on every surface, every variant).
- No further A2.8K repair required.

**Full verdict history (preserved, none overwritten):**
1. Original independent final audit (2026-09-24): `REPAIR_REQUIRED` — see `docs/plans/M35_URIV1_A2_8K_INDEPENDENT_FINAL_AUDIT.md`, unchanged file, hash `b3f19411b7a8d371e24d586901aeedc076f500f7ceecf7d6704693f722687c38`.
2. A2.8K-R1 bounded repair (2026-09-24): `VERIFICATION_READY` — see `docs/plans/M35_URIV1_A2_8K_R1_REPAIR_REPORT.md`, unchanged file, hash `4b2d6568a3b50369d50005c2d1e9c59616ebc188182594dcc95cb248b95b8489`.
3. R1 independent re-audit (2026-09-24, delivered as the A2.8K-R2 directive; no separate audit artifact file exists on disk — see A2.8K-R2 history entry below for what it found): `REPAIR_REQUIRED`.
4. A2.8K-R2 bounded repair (2026-09-24): `VERIFICATION_READY` — see `docs/plans/M35_URIV1_A2_8K_R2_REPAIR_REPORT.md`.
5. R2 independent audit (2026-09-24): `ACCEPT` — see `docs/plans/M35_URIV1_A2_8K_R2_INDEPENDENT_AUDIT.md` (new artifact filed this closeout; preserves the R1 provenance limitation — no standalone R1 audit file exists, only the directive text). **A2.8K CLOSED / ACCEPTED.**

Plan: [M35_URIV1_A2_8K_L5_EVIDENCE_SUFFICIENCY_PLAN.md](M35_URIV1_A2_8K_L5_EVIDENCE_SUFFICIENCY_PLAN.md)
Predecessor: A2.8J COMPLETE — [M35_URIV1_A2_8J_STATE.md](M35_URIV1_A2_8J_STATE.md) (closure commit 247265f733a4f199153089837c471f29b3725885, pushed)

### RECOVERY STATE
required_model: none (milestone closed)
current_owner: none — closed
resume_stage: COMPLETE
pause_reason: none — milestone closed and released
task: A2.8K Level-5 evidence sufficiency & attachment ordering diagnostic qualification — CLOSED
completed_steps: A2.8J closure; causal diagnosis of baseline Level 5/5.5; 168-row oracle failure-class inventory; competing hypotheses H0-H4 + diagnostics DX-1/DX-2 defined; surfaces S-A-S-E and pre-registered classification rules defined; User approval + §8 Q1-Q4 freeze recorded in plan §11; S-D fixtures authored+hashed before hypothesis code; rar_l5_experimental.py implemented (H1/H2/H3/DX-1); original execution report written, VERIFICATION_READY; **independent final audit: REPAIR_REQUIRED** (H3 rank not domain-relative; aggregate comparator incomplete); **A2.8K-R1 bounded repair**: domain-relative ranking (`_domain_relative_ranks`) + full decision-tuple aggregate accounting (`_decision_tuple`/`reconciliation_check`), VERIFICATION_READY; **R1 independent re-audit: REPAIR_REQUIRED** (delivered as the A2.8K-R2 directive) — R1's domain-relative re-ranking activated even when H3's lexical filter did not genuinely narrow the pool, fabricating a rank-0 winner baseline deliberately withheld (`test_rc1_current_conflicting_no_rank0_abstains`, and "the current document"); **A2.8K-R2 bounded repair implemented**: added `_h3_narrowed` activation-boundary check in `rar_l5_experimental.py` — domain-relative ranking (and the `has_ordering`-gate skip) now activates only when `_h3_domain`'s output id-set genuinely differs from the pool Level 5 would otherwise see; unnarrowed domains (including the zero-substantive-tokens case) fall back to baseline's exact global-rank semantics and `has_ordering` gate; no change to R1-B's aggregate logic (R1 re-audit found no defect there); reran the frozen battery twice (determinism confirmed) to new `_R2_TELEMETRY.json`/`_R2_AGGREGATES.json`; original AND R1 evidence files verified byte-unchanged by hash before and after; 9 new `H3ActivationBoundaryTests` added; `SD-C-02`/`NB-D-01` C2 (R1's genuine fixes) reconfirmed intact; the named S-E conflict (`test_rc1_current_conflicting_no_rank0_abstains`) confirmed cleared; exactly 2 rows (`NB-D-03` r1 C2, `NB-L-06` r1 C2, both S-C) revert from R1's `CORRECT_RESOLUTION` (achieved via the now-removed unsafe mechanism) to baseline-matching `MISSED_RESOLVABLE_CASE`, disclosed plainly as the correct, intended cost of the fix, not a new regression against H0 (H0 itself never resolved either row); repair report written answering only R2-supported claims
remaining_steps: none — milestone closed. Next: Claude inspects newly committed state and drafts a fresh plan for the following milestone.
changed_files: docs/plans/M35_URIV1_A2_8K_L5_EVIDENCE_SUFFICIENCY_PLAN.md, docs/plans/M35_URIV1_A2_8K_STATE.md, uri_v1/turn/rar_l5_diagnostic_fixtures.py, uri_v1/turn/rar_l5_experimental.py (R1-A + R2 activation-boundary repair), scripts/m35_a2_8k_l5_battery.py (R1-B repair, output paths bumped to _R2_ for this rerun), tests/test_m35_uriv1_a2_8k_l5_experimental.py (updated, +H3ActivationBoundaryTests), tests/test_m35_uriv1_a2_8k_r1_repair.py, docs/plans/M35_URIV1_A2_8K_TELEMETRY.json (original, unchanged), docs/plans/M35_URIV1_A2_8K_AGGREGATES.json (original, unchanged), docs/plans/M35_URIV1_A2_8K_EXECUTION_REPORT.md (original, unchanged), docs/plans/M35_URIV1_A2_8K_INDEPENDENT_FINAL_AUDIT.md (original, unchanged), docs/plans/M35_URIV1_A2_8K_R1_TELEMETRY.json (R1, unchanged), docs/plans/M35_URIV1_A2_8K_R1_AGGREGATES.json (R1, unchanged), docs/plans/M35_URIV1_A2_8K_R1_REPAIR_REPORT.md (R1, unchanged), docs/plans/M35_URIV1_A2_8K_R2_TELEMETRY.json, docs/plans/M35_URIV1_A2_8K_R2_AGGREGATES.json, docs/plans/M35_URIV1_A2_8K_R2_REPAIR_REPORT.md, docs/plans/M35_URIV1_A2_8K_R2_INDEPENDENT_AUDIT.md (new, this closeout)
git_state: closure commit made and pushed to `origin/master` (hash recorded in a follow-up governance commit, per the A2.8J precedent)
test_state: `tests/test_m35_uriv1_a2_8k_l5_experimental.py` (24 tests/66 subtests, +9 new H3ActivationBoundaryTests) + `tests/test_m35_uriv1_a2_8k_r1_repair.py` (6 tests) + all 4 existing A2.5 RAR test modules (104 tests/114 subtests total combined) -- all pass
audit_state: original independent final audit `REPAIR_REQUIRED` (preserved); R1 independent re-audit `REPAIR_REQUIRED` (preserved, delivered as the R2 directive, no standalone file); R2 independent audit `ACCEPT` (preserved, filed as `docs/plans/M35_URIV1_A2_8K_R2_INDEPENDENT_AUDIT.md` this closeout) — milestone closed on this verdict
last_successful_checkpoint: A2.8K closure commit pushed to `origin/master`
retry_metadata: preserve original, R1, and R2 evidence permanently; milestone closed, no further rerun expected

### History
- 2026-09-24: **Independent final audit verdict: `REPAIR_REQUIRED`.** Fresh reproduction matched all 3,636 saved S-A/S-B/S-C/S-D rows and all 19 S-E module runs, with two fresh runs decision-identical; focused regression passed 86 tests / 114 subtests. Protected hashes and the 18-fixture S-D hash matched. Two bounded defects prevent closure: (1) H3 filters the candidate domain but retains absolute full-pool rank values, so it does not faithfully compute ordinal position within the compatible domain; H4 inherits this defect; (2) aggregate changed-row logic omits outcome and ambiguous-candidate-only changes, hiding 6 H3 and 6 H4 decision changes, including four S-A `AMBIGUOUS`→`UNKNOWN` changes per variant. Mechanical rubric labels remain H1/H2/H3/H4 = `PARTIALLY SUPPORTED`, but H3/H4 are not hypothesis-valid until repaired and rerun. DX-1 establishes a sufficient tie intervention and partial clock causality, not sole root cause; `NB-C-05` C2 reflects a membership/domain/precedence interaction, not incomplete corpus `turn_attachments`. Freeze chronology is only `PARTIALLY VERIFIED`; the disclosed H3 pre-measurement repair is `INSUFFICIENT_EVIDENCE` because no immutable checkpoint exists. No implementation, fixture, telemetry, aggregate, execution-report, baseline, contract, D1RQ, or Level-6 file was changed; no commit/push/promotion. Full evidence: `docs/plans/M35_URIV1_A2_8K_INDEPENDENT_FINAL_AUDIT.md`.
- 2026-09-24: **A2.8K-R1 bounded repair complete, `VERIFICATION_READY`.** Implemented exactly the two corrections the independent audit required, nothing more: R1-A gives H3's `previous`/`earlier`/`latest`/`current` branches domain-relative ordinal ranking (`_domain_relative_ranks`, dense re-rank by the existing `recency_rank`/DX-1-adjusted value, ties preserved) instead of global rank, and additionally removes the now-redundant/invalid `has_ordering` gate for the `h3=True` path only (a singleton compatible domain is trivially its own domain's "latest," per the audit's own reasoning) — flag-off/H1/H2/DX-1 paths are untouched, verified by the existing and new equivalence tests. R1-B replaces the aggregate comparator's `(scoring_class, candidate_id)` shortcut with a full `(outcome, candidate_id, ambiguous_id_set)` comparison plus an independent `reconciliation_check`, closing the audit's 12-omitted-row gap (verified: the S-A regression check now correctly reports `False` for H3/H4, naming the audit's exact `NB-D-07`/`NB-L-04` rows). Reran the full 5-surface battery twice (determinism confirmed) to NEW `M35_URIV1_A2_8K_R1_TELEMETRY.json`/`_R1_AGGREGATES.json` — the original `_TELEMETRY.json`/`_AGGREGATES.json`/`_EXECUTION_REPORT.md`/`_INDEPENDENT_FINAL_AUDIT.md` are preserved byte-identical (verified by hash before and after). Result: H3 now achieves its actual intended binding on both of the audit's named examples (`SD-C-02` → `RESOLVED sd-c2-minutes`; `NB-D-01` C2 → `RESOLVED` to the correct candidate; both were `UNKNOWN` before this repair), pooled `CORRECT_RESOLUTION` rising 146→151 (H3) and 141→146 (H4) with the ICB count unchanged (12/6) -- zero new ICBs introduced by this repair. `SD-C-03` (St/Street, genuinely empty lexical domain), `NB-D-02` C1 (plan's own "coincidentally correct" row), and `SD-A-08` (a Level-5.5 lexical-narrowing gap) remain disclosed, unrepaired losses, correctly outside R1-A's scope. One NEW S-E conflict was found and disclosed, not silently patched: `test_rc1_current_conflicting_no_rank0_abstains` now fails under H3/H4, because dense re-ranking is applied to any H3-produced domain (per the audit's literal specification) even when that domain equals the full, unfiltered pool with ranks that don't start at 0 -- flagged for the next independent audit's judgment rather than resolved by inventing an unrequested "only re-rank a genuinely narrowed domain" carve-out. DX-1's and H1's/H2's own numbers are byte-identical to the pre-repair measurement (untouched by either fix). Mechanical classification remains H1/H2/H3/H4 = `PARTIALLY SUPPORTED`; none promoted to `SUPPORTED`, `ACCEPTED`, or any equivalent status. No baseline/contract/RAR-SAFE/D1RQ/corpus/S-D-fixture file was modified (hashes identical pre/post, matching the independent audit's own recorded values). No commit, no push, no self-acceptance. Full evidence: `docs/plans/M35_URIV1_A2_8K_R1_REPAIR_REPORT.md`, `_R1_TELEMETRY.json`, `_R1_AGGREGATES.json`. **Independent re-audit of this repair is required before any promotion, acceptance, or next experiment.**
- 2026-09-24: **R1 independent re-audit verdict: `REPAIR_REQUIRED`** (delivered as the A2.8K-R2 task directive; no separate audit artifact file was produced on disk for this pass — the directive's own "Starting Verdict" section is the authoritative record). Findings: R1 correctly fixed genuine-narrowing domain-relative ranking and full decision-transition accounting, and both were independently re-confirmed working. But R1's activation of dense/domain-relative ranking was gated on the bare `h3` flag rather than on whether `_h3_domain` actually excluded anything, so it also fired when the lexical filter changed nothing -- fabricating a rank-0 winner in pools baseline deliberately left without one. Reproduced: baseline `UNKNOWN` / R1 H3 `RESOLVED` for `test_rc1_current_conflicting_no_rank0_abstains`; same defect shape for "the current document" with no lexical restriction narrowing the domain.
- 2026-09-24: **A2.8K-R2 bounded repair complete, `VERIFICATION_READY`.** Implemented exactly one activation-boundary rule in `uri_v1/turn/rar_l5_experimental.py`: `_h3_narrowed = h3 and ({ids in H3-filtered pool} != {ids in the pool Level 5 would otherwise see})`, computed by candidate-id-set membership (not list length, per the directive's explicit caution). Domain-relative re-ranking (`_domain_rank_map`) and the `latest`/`current` branch's `has_ordering`-gate skip now both key off `_h3_narrowed` instead of the bare `h3` flag; when the domain is not genuinely narrowed -- including the zero-substantive-tokens case, which `_h3_domain` already returns unchanged, so no separate flag was needed -- baseline's exact global-rank semantics and `has_ordering` gate apply. No change to R1-B's aggregate-accounting logic (the R1 re-audit found no defect there). Added 9 new `H3ActivationBoundaryTests` covering the directive's Case A (genuine narrowing, gapped ranks), Case B (unchanged domain, the exact `test_rc1_current_conflicting_no_rank0_abstains` shape, and "the current document"), Case C (no substantive tokens), determinism under both regimes, and R1 non-regression (`SD-C-02`/`NB-D-01` C2). Reran the frozen 5-surface battery twice (determinism confirmed) to NEW `M35_URIV1_A2_8K_R2_TELEMETRY.json`/`_R2_AGGREGATES.json` -- the original AND R1 evidence files are preserved byte-identical (hash-verified before and after). Result: the named S-E conflict is cleared (`test_rc1_current_conflicting_no_rank0_abstains` now passes under H3); `SD-C-02`/`NB-D-01` C2 remain correctly resolved (R1's genuine fix intact); reconciliation holds for every variant (H3 45/45, H4 56/56, others unchanged); S-A regression check remains `False` for H3/H4 for the same 4 genuinely-narrowed `NB-D-07`/`NB-L-04` rows (unaffected by this repair, confirmed not instances of the activation-boundary defect); zero new ICBs on any surface for any variant (unchanged). Exactly 2 rows (`NB-D-03` r1 C2, `NB-L-06` r1 C2, both S-C, both variants H3/H4) revert from R1's `CORRECT_RESOLUTION` -- achieved via the now-removed unsafe mechanism -- to baseline-matching `MISSED_RESOLVABLE_CASE`; disclosed plainly as the correct, intended cost of the fix (H0 itself never resolved either row), not a new regression. Pooled `CORRECT_RESOLUTION` accordingly 151→149 (H3) and 146→144 (H4); ICB counts unchanged (12/6). Mechanical classification remains H1/H2/H3/H4 = `PARTIALLY SUPPORTED`; none promoted, none accepted, A2.8K not closed. No baseline/contract/RAR-SAFE/D1RQ/corpus/S-D-fixture file was modified (hashes identical pre/post, matching all prior recorded values). No commit, no push, no self-acceptance. Full evidence: `docs/plans/M35_URIV1_A2_8K_R2_REPAIR_REPORT.md`, `_R2_TELEMETRY.json`, `_R2_AGGREGATES.json`. **Independent re-audit of R2 is required before any promotion, acceptance, or next experiment.**
- 2026-09-24: **R2 independent audit verdict: `ACCEPT`.** Independent auditor verified branch/HEAD, reproduced all 3,636 decision rows + 19 S-E records across two fresh in-memory runs with zero mismatches, passed 104 tests/114 subtests, and matched all six protected file hashes plus original/R1 evidence hashes. Confirmed the R2 activation-boundary repair is valid: domain-relative ranking activates only on genuine candidate-id-set narrowing (590 narrowed calls all reranked, 460 unchanged calls none reranked, zero activation-boundary mismatches); `SD-C-02`/`NB-D-01` C2 remain correctly resolved; the R1→R2 diff is exactly the 4 disclosed rows (`NB-D-03` r1 C2, `NB-L-06` r1 C2, ×2 variants), confirmed as legitimate reversions, not new regressions against H0. Independent decision accounting reconciled exactly with the R2 aggregates for all six variants across all 606 decision keys (H1 11/6/0/5, H2 8/6/0/2, H3 45/14/0/15, H4 56/20/0/20, DX-1 5/5/0/0 — changed/closed-ICB/new-ICB/lost-correct). H3 149 correct resolutions/12 ICBs, H4 144/6. Frozen-rubric classification for all four: `PARTIALLY_SUPPORTED`. Zero new ICBs on any surface, any variant. Findings recorded verbatim, unmodified, in the new artifact `docs/plans/M35_URIV1_A2_8K_R2_INDEPENDENT_AUDIT.md` (filed at closeout; no equivalent artifact existed for R1's re-audit, that provenance limitation preserved and disclosed, not inherited by R2). No further A2.8K repair required.
- 2026-09-24: **A2.8K CLOSED / ACCEPTED.** Closeout performed per User directive following the R2 independent `ACCEPT` verdict. Final qualified result: H1/H2/H3/H4 = `PARTIALLY_SUPPORTED`; R2 repaired H3's activation-boundary semantics; decision accounting complete; determinism verified; no new ICBs introduced; no further A2.8K repair required. All original, R1, and R2 evidence preserved unchanged (byte-identical, hash-verified); no frozen fixture or contract modified; no residual failure reinterpreted. Minimum verification for closure: full A2.8K + applicable A2.5 test suite rerun (104 tests/114 subtests, all pass) and protected-file hash spot-check, both against the same repository state audited by R2's independent auditor (HEAD `925de8f7`, no changes since). Committed and pushed to `origin/master` per standing AO-4 release authority (Claude Code Architect/Final Auditor/Release Authority role) — commit hash recorded in a follow-up governance commit per the A2.8J precedent. A2.8L and any new experiment explicitly not started.
- 2026-09-24: Plan drafted by Claude after A2.8J closure under User authorization "A2.8K PLANNING ONLY". Stopped at PLAN_READY_FOR_USER_APPROVAL. No implementation.
- 2026-09-24: User approved experimental implementation via directive (Codex preferred/Antigravity coordinator/Claude Sonnet fallback-1). Since Codex and Antigravity run entirely outside the Claude Code session and cannot be invoked or simulated from it (standing governance), and since Claude is not the default AO-4 implementer role, Claude asked the User directly how to proceed; the User selected "Claude implements as fallback 1". Repository/branch verified (`m35-uri-v1-parallel-architecture`, HEAD `925de8f7`) against the directive's expected state before any change. §8 Q1-Q4 frozen and recorded in plan §11. State moved to IMPLEMENTING.
- 2026-09-24: **S-D frozen before any hypothesis code.** `uri_v1/turn/rar_l5_diagnostic_fixtures.py` authored: 18 `RARFixture` rows (8 Class A attachment identity/ordering incl. Q2 Case-A/Case-B trustworthy-clock twins and a lexically-distinguished-attachment row; 2 Class B absent-entity twin; 3 Class C ordering-domain incl. the St/Street known-limitation pair; 2 Class D DX-2 relative-anchor measure-only rows; 3 Class N negative controls). Per-row DX-1 tie-ids and DX-2 measure-only flags recorded separately in the same file's `L5_DIAGNOSTIC_METADATA` dict (not added to the frozen `RARCandidate`/`RARQuery`/`RARFixture` contracts). SHA-256 of the frozen fixture file: `ce25cd76c1652284145dc95c3d3037c6e784fe51f92a97ace6e67e7fdf371148`. No hypothesis code (`rar_l5_experimental.py`) exists yet at this checkpoint.
- 2026-09-24: **Batch complete, VERIFICATION_READY.** `uri_v1/turn/rar_l5_experimental.py` implemented (H1/H2/H3/H4/DX-1, all scoped to Level 5/5.5 only, byte-identical elsewhere). Flag-off equivalence verified at unit scale and at S-A corpus scale (0 diffs vs. H0 for every variant). A defect found by the H3 mechanism test during implementation (empty compatible-domain case incorrectly returning baseline's unrelated full-pool INSUFFICIENT_METADATA abstention instead of truly falling through past Level 5) was fixed before any battery run, with a dedicated regression test added; this is disclosed in the execution report, not hidden. Full 5-surface battery (`scripts/m35_a2_8k_l5_battery.py`) run twice: determinism confirmed, S-A regression-free for every variant, zero new ICBs introduced by any variant on any surface. Results: H1/H2/H3/H4 each classified **PARTIALLY SUPPORTED** against the plan's pre-registered rules (each closes its full target-class ICB set with zero new ICBs, but each also loses at least one correct resolution not permitted to abstain by its frozen semantics); H4 accumulates the union of every component's costs and benefits (largest ICB-closure count and largest loss count, tied at 20 each), confirming the components are additive rather than complementary. DX-1 (diagnostic only) closes 5 of 6 Class-A ICB instances at zero cost, identifying the `created_at` rank clock — not cascade precedence — as the dominant root cause of Class A, and separately surfaces that the corpus's own `turn_attachments` field does not fully describe every candidate-pool cell (`NB-C-05` C2 unresolved by DX-1). The batch's central, previously-unmeasured discovery is a genuine architecture-level interaction: baseline Level 6's substantive-token computation does not exclude the same R3(a) recency vocabulary H1/H3 exclude, so H1/H3 fall-throughs on recency-worded queries frequently land on an uninformative UNKNOWN via Level 6's absent-entity path rather than the intended correct binding or safe abstention family — recorded as unmeasured-follow-up-hypothesis #1 in the execution report, not implemented (would require touching Level 6, outside this batch's frozen scope). Full detail: `docs/plans/M35_URIV1_A2_8K_EXECUTION_REPORT.md`, `_TELEMETRY.json`, `_AGGREGATES.json`. No baseline/contract/RAR-SAFE/D1RQ/corpus file was modified (SHA-256 identical pre/post, verified on both runs). No commit, no push, no self-acceptance -- state set to `VERIFICATION_READY` per directive §24, awaiting independent audit.
