# Copy this file to .env (already gitignored - never commit real keys)
# and fill in real values. uri_core/tools/web_search.py loads .env
# automatically; a real environment variable of the same name always
# takes priority over this file.

# Milestone 15 Part 1: web-search capability (uri_core/tools/web_search.py).
# Get a free key at https://tavily.com - no credit card required.
TAVILY_API_KEY=
